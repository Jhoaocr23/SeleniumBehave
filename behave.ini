[behave]
paths = features
color = true
stdout_capture = no
stderr_capture = no
log_capture = no
format = pretty

[behave.userdata]
allure_report_dir = reports/allure-results

[behave.formatters]
allure = allure_behave.formatter:AllureFormatter
